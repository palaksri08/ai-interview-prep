def generate_questions_prompt(resume_text, job_role):
    return f"Mock prompt for {job_role}"

def feedback_prompt(question, answer, job_role):
    return f"Mock feedback prompt for question: {question}"
